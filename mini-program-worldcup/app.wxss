/* CHO的世界杯小站 - Theme Token System */
/* WXSS doesn't support CSS variables, using utility classes */

/* ===== Background Colors ===== */
.bg-dark { background-color: #080c14; }
.bg-surface { background-color: #0f1521; }
.bg-surface2 { background-color: #182032; }
.bg-card { background-color: #0f1521; }
.bg-accent-subtle { background-color: rgba(77,166,255,.04); }

/* ===== Text Colors ===== */
.text-primary { color: #e4e8f0; }
.text-secondary { color: #8899b4; }
.text-muted { color: #556680; }
.text-accent { color: #4da6ff; }
.text-gold { color: #f0b90b; }
.text-green { color: #00c853; }
.text-red { color: #ff3d57; }
.text-orange { color: #ff9100; }
.text-purple { color: #8b5cf6; }

/* ===== Border ===== */
.border-default { border: 1px solid #1a2744; }
.border-accent { border-color: #4da6ff; }
.border-gold { border-color: #f0b90b; }

/* ===== Radius ===== */
.radius-sm { border-radius: 8px; }
.radius { border-radius: 12px; }
.radius-lg { border-radius: 16px; }

/* ===== Shadows ===== */
.shadow-card { box-shadow: 0 4px 32px rgba(0,0,0,.4); }

/* ===== Flex Layout ===== */
.flex { display: flex; }
.flex-center { display: flex; align-items: center; justify-content: center; }
.flex-between { display: flex; align-items: center; justify-content: space-between; }
.flex-wrap { flex-wrap: wrap; }
.flex-1 { flex: 1; }
.gap-sm { gap: 6px; }
.gap { gap: 10px; }
.gap-md { gap: 16px; }

/* ===== Typography ===== */
.font-sm { font-size: 20rpx; }
.font-xs { font-size: 22rpx; }
.font-s { font-size: 24rpx; }
.font-m { font-size: 28rpx; }
.font-l { font-size: 32rpx; }
.font-xl { font-size: 36rpx; }
.font-xxl { font-size: 44rpx; }
.font-bold { font-weight: 700; }
.font-extrabold { font-weight: 800; }
.text-center { text-align: center; }
.text-right { text-align: right; }

/* ===== Page ===== */
page {
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'PingFang SC', 'Microsoft YaHei', sans-serif;
  background-color: #080c14;
  color: #e4e8f0;
  line-height: 1.5;
  font-size: 28rpx;
}

/* ===== Header ===== */
.header {
  background: linear-gradient(135deg, #0a1628, #132044);
  border-bottom: 2px solid #4da6ff;
  padding: 0 20px;
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 62px;
  position: sticky;
  top: 0;
  z-index: 100;
}

/* ===== Card ===== */
.card {
  background: #0f1521;
  border: 1px solid #1a2744;
  border-radius: 12px;
  padding: 18px;
  box-shadow: 0 4px 32px rgba(0,0,0,.4);
  margin-bottom: 16px;
}

.card-header {
  font-size: 32rpx;
  font-weight: 700;
  margin-bottom: 14px;
  display: flex;
  align-items: center;
  gap: 8px;
  padding-bottom: 10px;
  border-bottom: 1px solid #1a2744;
}

/* ===== Button ===== */
.btn {
  padding: 9px 22px;
  border: none;
  border-radius: 8px;
  font-size: 28rpx;
  font-weight: 700;
}

.btn-primary {
  background: #4da6ff;
  color: #fff;
}

.btn-outline {
  background: transparent;
  color: #4da6ff;
  border: 1px solid #4da6ff;
}

.btn-sm {
  padding: 5px 12px;
  font-size: 24rpx;
}

/* ===== Match Card ===== */
.match-card {
  background: #182032;
  border: 1px solid #1a2744;
  border-radius: 12px;
  padding: 16px;
  margin-bottom: 10px;
}

.match-card.featured {
  border-left: 3px solid #f0b90b;
}

.match-card .mc-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 12px;
}

.match-card .mc-time {
  font-size: 24rpx;
  color: #556680;
  font-weight: 600;
}

.match-card .mc-group {
  font-size: 22rpx;
  background: #4da6ff;
  color: #fff;
  padding: 2px 8px;
  border-radius: 10px;
  font-weight: 700;
}

.match-card .mc-teams {
  display: flex;
  align-items: center;
  gap: 14px;
}

.match-card .mc-team {
  flex: 1;
  text-align: center;
}

.match-card .mc-flag {
  font-size: 70rpx;
  display: block;
  margin-bottom: 4px;
}

.match-card .mc-name {
  font-weight: 700;
  font-size: 30rpx;
}

.match-card .mc-rank {
  font-size: 22rpx;
  color: #8899b4;
}

.match-card .mc-vs {
  font-size: 38rpx;
  font-weight: 800;
  color: #556680;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 4px;
}

.match-card .mc-pred-score {
  font-size: 44rpx;
  font-weight: 800;
  color: #f0b90b;
}

.match-card .mc-probs {
  display: flex;
  gap: 4px;
  justify-content: center;
  font-size: 22rpx;
  margin-top: 8px;
}

/* ===== Schedule ===== */
.schedule-day {
  margin-bottom: 20px;
}

.schedule-day .sd-header {
  padding: 10px 14px;
  background: #182032;
  border-radius: 8px;
  font-weight: 700;
  font-size: 28rpx;
  margin-bottom: 8px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.schedule-match {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 9px 12px;
  background: #0f1521;
  border-radius: 8px;
  margin-bottom: 3px;
  font-size: 26rpx;
}

.schedule-match .sm-time {
  font-size: 24rpx;
  color: #556680;
  min-width: 42px;
}

.schedule-match .sm-teams {
  flex: 1;
}

.schedule-match .sm-group {
  font-size: 22rpx;
  background: #182032;
  padding: 2px 8px;
  border-radius: 8px;
  color: #8899b4;
}

.schedule-match .sm-pred {
  color: #f0b90b;
  font-weight: 600;
  font-size: 24rpx;
  min-width: 40px;
  text-align: center;
}

.schedule-match .sm-actual {
  color: #00c853;
  font-weight: 700;
  font-size: 26rpx;
  min-width: 40px;
  text-align: center;
}

.schedule-match .sm-pending {
  color: #556680;
  font-weight: 600;
  font-size: 24rpx;
  min-width: 40px;
  text-align: center;
}

/* ===== Team Grid ===== */
.team-card {
  background: #182032;
  border: 1px solid #1a2744;
  border-radius: 12px;
  padding: 14px;
}

.team-card .tc-header {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 8px;
}

.team-card .tc-flag {
  font-size: 60rpx;
}

.team-card .tc-name {
  font-weight: 700;
  font-size: 28rpx;
}

.team-card .tc-stars {
  font-size: 22rpx;
  color: #f0b90b;
}

.team-card .tc-group {
  font-size: 22rpx;
  background: #4da6ff;
  color: #fff;
  padding: 2px 10px;
  border-radius: 8px;
}

/* ===== Stat Box ===== */
.stat-box {
  background: #182032;
  border-radius: 8px;
  padding: 14px;
  text-align: center;
}

.stat-box .num {
  font-size: 54rpx;
  font-weight: 800;
  color: #4da6ff;
}

.stat-box .num.gold {
  color: #f0b90b;
}

.stat-box .lbl {
  font-size: 24rpx;
  color: #8899b4;
  margin-top: 2px;
}

/* ===== Duel ===== */
.duel-scoreboard {
  display: grid;
  grid-template-columns: 1fr auto 1fr;
  gap: 12px;
  text-align: center;
  padding: 16px;
  background: #182032;
  border-radius: 12px;
  margin-bottom: 14px;
}

/* ===== News Ticker ===== */
.news-ticker {
  background: linear-gradient(90deg, #1a0a0a, #0a0a1a);
  border-bottom: 1px solid #1a2744;
  padding: 6px 20px;
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 24rpx;
  height: 32px;
}

.news-ticker .ticker-label {
  background: #ff3d57;
  color: #fff;
  padding: 2px 10px;
  border-radius: 10px;
  font-weight: 800;
  font-size: 22rpx;
  white-space: nowrap;
}

.news-ticker .ticker-content {
  flex: 1;
  white-space: nowrap;
  color: #8899b4;
  overflow: hidden;
  position: relative;
  height: 20px;
}

/* ===== Modal/Dialog ===== */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,.75);
  z-index: 200;
  display: flex;
  align-items: center;
  justify-content: center;
}

.modal {
  background: #0f1521;
  border: 1px solid #1a2744;
  border-radius: 12px;
  padding: 24px;
  max-width: 620px;
  width: 92%;
  max-height: 85vh;
  overflow-y: auto;
}

/* ===== Empty State ===== */
.empty-state {
  text-align: center;
  padding: 50px 20px;
  color: #556680;
}

.empty-state .icon {
  font-size: 96rpx;
  margin-bottom: 10px;
}

/* ===== Progress Bar ===== */
.progress-wrap {
  height: 8px;
  background: #182032;
  border-radius: 4px;
  overflow: hidden;
  margin: 8px 0;
}

.progress-fill {
  height: 100%;
  background: linear-gradient(90deg, #4da6ff, #8b5cf6);
  border-radius: 4px;
  transition: width .3s;
}

/* ===== Group Tabs ===== */
.group-tabs {
  display: flex;
  gap: 3px;
  flex-wrap: wrap;
  margin-bottom: 16px;
}

.group-tab {
  padding: 5px 12px;
  border: 1px solid #1a2744;
  background: transparent;
  color: #8899b4;
  border-radius: 20px;
  font-size: 24rpx;
  font-weight: 700;
}

.group-tab.active {
  background: #4da6ff;
  border-color: #4da6ff;
  color: #fff;
}

/* ===== Scroll Area ===== */
.scroll-y {
  overflow-y: auto;
}

/* ===== Utility ===== */
.truncate {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
