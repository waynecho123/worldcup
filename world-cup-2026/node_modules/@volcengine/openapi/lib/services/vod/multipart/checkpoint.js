"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.validateCheckpoint = exports.removeCheckpoint = exports.saveCheckpoint = exports.loadCheckpoint = exports.getDefaultCheckpointFilePath = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
const util_1 = require("util");
const fsStat = (0, util_1.promisify)(fs_1.default.stat);
const fsReadFile = (0, util_1.promisify)(fs_1.default.readFile);
const fsWriteFile = (0, util_1.promisify)(fs_1.default.writeFile);
const fsMkdir = (0, util_1.promisify)(fs_1.default.mkdir);
const fsUnlink = (0, util_1.promisify)(fs_1.default.unlink);
/**
 * 生成默认 checkpoint 文件路径
 */
function getDefaultCheckpointFilePath(spaceName, filePath) {
    const crypto = require("crypto");
    const hash = crypto.createHash("md5").update(`${spaceName}.${filePath}`).digest("hex");
    const fileName = path_1.default.basename(filePath);
    const normalizedPath = `${fileName}.${hash}.upload`;
    return normalizedPath.replace(/[\\/]/g, "");
}
exports.getDefaultCheckpointFilePath = getDefaultCheckpointFilePath;
/**
 * 加载 checkpoint
 */
async function loadCheckpoint(checkpoint) {
    if (!checkpoint) {
        return {};
    }
    // 如果是对象，直接返回
    if (typeof checkpoint === "object") {
        return { record: checkpoint };
    }
    // 如果是文件路径
    if (typeof checkpoint === "string") {
        try {
            const stat = await fsStat(checkpoint);
            const isDirectory = stat.isDirectory() || checkpoint.endsWith("/");
            if (isDirectory) {
                // 目录路径，需要生成文件名（实际使用时需要传入完整路径）
                throw new Error("Directory checkpoint path not supported. Please provide a full file path.");
            }
            // 读取 checkpoint 文件
            const content = await fsReadFile(checkpoint, "utf-8");
            const record = JSON.parse(content);
            return { filePath: checkpoint, record };
        }
        catch (err) {
            // 文件不存在，将创建新的 checkpoint
            if (err.code === "ENOENT") {
                return { filePath: checkpoint };
            }
            throw err;
        }
    }
    return {};
}
exports.loadCheckpoint = loadCheckpoint;
/**
 * 保存 checkpoint
 */
async function saveCheckpoint(filePath, record) {
    if (!filePath) {
        return;
    }
    try {
        const dirPath = path_1.default.dirname(filePath);
        if (dirPath !== "." && dirPath !== "") {
            await fsMkdir(dirPath, { recursive: true });
        }
        await fsWriteFile(filePath, JSON.stringify(record, null, 2), "utf-8");
    }
    catch (err) {
        console.warn("保存 checkpoint 文件失败:", err);
    }
}
exports.saveCheckpoint = saveCheckpoint;
/**
 * 删除 checkpoint
 */
async function removeCheckpoint(filePath) {
    if (!filePath) {
        return;
    }
    try {
        await fsUnlink(filePath);
    }
    catch (err) {
        if (err.code !== "ENOENT") {
            console.warn("删除 checkpoint 文件失败:", err.message);
        }
    }
}
exports.removeCheckpoint = removeCheckpoint;
/**
 * 校验 checkpoint 有效性
 */
async function validateCheckpoint(record, filePath, fileSize, partSize) {
    if (!record) {
        return false;
    }
    // 校验文件信息（仅文件路径上传）
    if (filePath && record.file_info) {
        try {
            const stat = await fsStat(filePath);
            if (stat.mtimeMs !== record.file_info.last_modified ||
                stat.size !== record.file_info.file_size) {
                console.warn("文件已被修改，checkpoint 无效");
                return false;
            }
        }
        catch (err) {
            console.warn("无法读取文件信息，checkpoint 无效");
            return false;
        }
    }
    // 校验分片大小
    if (partSize && record.part_size !== partSize) {
        console.warn("分片大小不匹配，checkpoint 无效");
        return false;
    }
    // 校验文件大小（如果提供）
    if (fileSize && record.total_size !== fileSize) {
        console.warn("文件大小不匹配，checkpoint 无效");
        return false;
    }
    return true;
}
exports.validateCheckpoint = validateCheckpoint;
//# sourceMappingURL=checkpoint.js.map