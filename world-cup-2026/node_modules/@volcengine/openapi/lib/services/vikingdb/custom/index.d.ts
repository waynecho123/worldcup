import { AbstractService } from "../abstractService";
import { CustomResponse, type Options } from "./types";
export declare class CustomService extends AbstractService {
    fetch<T extends NonNullable<unknown>, R = void>({ pathname, data, ...rest }: Options<T>): Promise<CustomResponse<R>>;
}
